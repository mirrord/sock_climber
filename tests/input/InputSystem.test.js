import { describe, it, expect } from 'vitest';

describe('InputSystem', () => {
  it.todo('produces immutable snapshot each frame');
  it.todo('buffers inputs between frames');
  it.todo('maps raw keys to actions');
});
