import { describe, it, expect } from 'vitest';

describe('GameLoop', () => {
  it.todo('calls update with fixed dt');
  it.todo('calls render with interpolation alpha');
  it.todo('accumulates time correctly across frames');
});
